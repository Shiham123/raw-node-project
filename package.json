{
	"name": "raw-node-project",
	"version": "1.0.0",
	"description": "this is raw node project",
	"main": "index.js",
	"scripts": {
		"start": "node index.js",
		"dev": "nodemon index.js",
		"production": "SET \"NODE_ENV=production\" && nodemon index",
		"staging": "SET \"NODE_ENV=staging\" && nodemon index",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "shiham",
	"license": "ISC",
	"devDependencies": {
		"eslint": "^8.57.0",
		"nodemon": "^3.1.0",
		"prettier": "^3.2.5"
	}
}
